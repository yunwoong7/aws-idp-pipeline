# Local development environment variables
# Only enable auth disable for local development
NEXT_PUBLIC_AUTH_DISABLED=true
NEXT_PUBLIC_BACKEND_PORT=8000

# Note: In production/ECS, NEXT_PUBLIC_AUTH_DISABLED should be 'false' or undefined